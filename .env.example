# Database-verbinding voor de PHP-app
#
# Platform:                    DB_HOST waarde:
# ──────────────────────────────────────────────
# Docker Compose (standaard):  db
# PHP lokaal op host (Linux/Windows): 127.0.0.1:3308
#
DB_HOST=db
DB_NAME=theater_aurora
DB_USER=root
DB_PASSWORD=rootpassword
